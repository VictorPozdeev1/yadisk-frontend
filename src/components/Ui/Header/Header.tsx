import React, { FC } from "react";
import style from "./Header.module.css";

const Header: FC<any> = () => {
  return <div className={style["Header-wrapper"]}>Header</div>;
};

export default Header;
