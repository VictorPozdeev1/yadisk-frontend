interface Document {
    id: string; // Уникальный идентификатор документа
    name: string; // Имя документа
    category: string; // Имя категории, к которой документ принадлежит
    url: string; // URL изображения документа (png)
}
